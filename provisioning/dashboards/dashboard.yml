apiVersion: 1

providers:
  - name: 'Default'
    folder: 'Finance'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards
